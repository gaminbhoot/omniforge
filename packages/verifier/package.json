{
  "name": "@omniforge/verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spec verifier — 10 automated checks incl. HITL integrity, sandbox isolation, and secrets hygiene",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "verify": "tsx src/index.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "@types/node": "^22.7.0"
  },
  "exports": {
    ".": "./src/index.ts",
    "./spec": "./src/spec.ts",
    "./git": "./src/git.ts"
  }
}
