## Description

<!-- Brief summary of what changes were made and why -->

## Modules Modified

- [ ] `apps/web` (Mission Control UI)
- [ ] `apps/server` (Orchestrator + HITL policy engine)
- [ ] `packages/mcp-tools` (MCP tool servers)
- [ ] `packages/sandbox` (Docker isolation runtime)
- [ ] `packages/verifier` (Spec verifier)
- [ ] Other / build / CI

## Verification Performed

<!-- Commands run and results, e.g. build, lint, tests, verifier, sandbox smoke test -->

- [ ] `npm run build` passes with 0 errors
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run verify` reports PASS
- [ ] Qodo review completed with 0 unresolved critical findings
