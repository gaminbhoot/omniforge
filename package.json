{
  "name": "omniforge",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous multi-agent mission control platform with sandboxed execution and human-in-the-loop governance",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/mcp-tools/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:server": "npm --workspace apps/server run dev",
    "dev:sandbox": "docker compose up --build sandbox",
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,py}\"",
    "test": "npm run test --workspaces --if-present",
    "prepare": "husky install || true",
    "verify": "tsx scripts/codex-monitor.ts --once",
    "postinstall": "node scripts/patch-anthropic-eager.js || true"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.67.0",
    "@typescript-eslint/parser": "^8.67.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.2",
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gaminbhoot/omniforge.git"
  },
  "license": "MIT",
  "dependencies": {
    "@truefoundry/trueforge": "^0.1.4"
  }
}
